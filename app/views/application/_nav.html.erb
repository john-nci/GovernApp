<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display
    -->
    <div class="navbar-header">
      <button aria-expanded="false" class="navbar-toggle collapsed" data-target="#bs-example-navbar-collapse-1" data-toggle="collapse" type="button">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Govern", root_path, class: "navbar-brand" %>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling
    -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active">
          <%= link_to "Polls", polls_path %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li></li>
          <span class="navbar-text">Signed in as #{current_user.email}</span>
          <li>
            <%= link_to destroy_session_path('user'), method: :delete do  %>
              <span class="glyphicon glyphicon-log-out"></span>
              Sign Out
            <% end %>
          </li>
        <% else %>
          <li>
            <%= link_to new_session_path('user') do %>
              <span class="glyphicon glyphicon-log-in"></span>
              Sign In
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
